<h2>Listing Products</h2>

<% @products.each do |pro| %></br>

	NAME - <%=link_to pro["name"], product_path(pro["id"])%></br>
	PRICE - <%= pro["price"] %>
	
<%= link_to 'Edit', edit_product_path(pro["id"]) %></br>

<% end %>	

<%= link_to 'ADD', new_product_path %>